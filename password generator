import random
import string

length = int(input("enter password legth: "))

chars = string.ascii_letters + string.digits + string.puctuation
password ="".join(random.choice(chars) for _ in range(legth))

print("genrated password;",password)
